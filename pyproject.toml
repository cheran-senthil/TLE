[project]
name = "TLE"
version = "0.2.0"
requires-python = ">= 3.9"
dependencies = [
  # Discord bot core
  "discord.py == 2.1.1",
  "aiohttp < 3.8",
  "aiocache",
  # Data & plotting
  "numpy",
  "pandas",
  "matplotlib",
  "seaborn",
  # Images
  "pillow",
]

[project.optional-dependencies]
test = [
  "pytest",
]
